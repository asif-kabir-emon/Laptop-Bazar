import React from "react";
import useTitle from "../../Hooks/useTitle";

const Register = () => {
  useTitle("Sign Up");
  return (
    <div>
      <h2>Register</h2>
    </div>
  );
};

export default Register;
